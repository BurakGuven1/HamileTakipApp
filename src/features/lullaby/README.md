# Lullaby

App-side API is in `src/api/lullabies.ts`.

Audio files should live in the public-read `lullabies` bucket. Offline playback can cache files through Expo FileSystem after the audio player UI is expanded.
